C++ Code:
#include<iostream>
#include<string>
using namespace std;
bool isRotation(string s1,string s2)
{
    if(s1.length()!=s2.length()) return false;
    string combined=s1+s2;
    int position=combined.find(s2);
    if(position<combined.length())
    {
        return true;
    } 
    else
    {
        return false;
    }
}
int main()
{
    string s1,s2;
    cin>>s1>>s2;
    bool ans=isRotation(s1,s2);
    if(ans) cout<<"True"<<endl;
    else cout<<"False"<<endl;
}

Input:
abcde deabc

Output:
True
